name: pulumi-deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        working-directory: ./ppinfra
        run: npm install

      - name: Install Pulumi AWS Plugin
        run: pulumi plugin install resource aws 6.83.2

      - name: Pulumi Up using ESC (OIDC)
        uses: pulumi/actions@v5
        with:
          command: up
          stack-name: ishwarya-dc-org/myworkshop/dev
          work-dir: ./ppinfra
        env:
          PULUMI_ACCESS_TOKEN: ${{ secrets.PULUMI_ACCESS_TOKEN }}
          PULUMI_ENV: ishwarya-dc-org/lab8-pulumi/aws-oidc-env   # ✅ FIXED
